rawa7el/
├── apps/
│   ├── bedaya/              # منصة بداية (الكود الحالي)
│   └── taht-el-eshreen/     # منصة تحت العشرين
├── packages/
│   ├── database/            # Prisma schema + client
│   ├── exam-logic/          # منطق الامتحانات (Headless)
│   ├── resources-logic/     # منطق الموارد (Headless)
│   ├── activities-logic/    # منطق الأنشطة (Headless)
│   ├── attendance-logic/    # منطق الحضور (Headless)
│   ├── notifications-logic/ # منطق الإشعارات (Headless)
│   ├── halaqat-logic/       # منطق الحلقات (لتحت العشرين)
│   ├── ui/                  # مكونات UI مشتركة
│   └── config/              # TypeScript + ESLint configs
├── turbo.json
├── package.json
└── pnpm-workspace.yaml